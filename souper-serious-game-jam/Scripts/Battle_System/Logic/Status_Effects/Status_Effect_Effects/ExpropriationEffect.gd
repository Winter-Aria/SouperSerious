extends StatusEffectEffect
class_name ExpropriationEffect

func apply_effect_to_action(action_data: ActionData, effect_stacks: int) -> ActionData:
	# Need code to delete one random card from the player's hand at the beginning of each turn
	return action_data
